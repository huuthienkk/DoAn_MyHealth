name: giamsat_suckhoe
description: "Ứng dụng giám sát sức khỏe & nhận diện món ăn."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  # 🔥 Firebase
  firebase_core: ^4.2.0
  firebase_auth: ^6.1.1
  cloud_firestore: ^6.0.3
  firebase_storage: ^13.0.3
  firebase_messaging: ^16.0.3

  # 📊 UI & Charts
  fl_chart: ^0.70.0
  google_fonts: ^6.2.1

  # 🔔 Local Storage & Notifications
  shared_preferences: ^2.1.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_local_notifications: ^19.5.0
  timezone: ^0.10.1

  # ⚙️ Tiện ích
  provider: ^6.1.2
  intl: ^0.18.1
  image_picker: ^1.2.0
  image: ^4.0.17

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    # 🧩 Thêm mô hình AI đã train
    - assets/food_model.tflite
    - assets/labels.txt